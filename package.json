{
  "name": "patri-mentor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others -n backend,frontend -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm start",
    "ingest": "cd backend && npm run ingest"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
